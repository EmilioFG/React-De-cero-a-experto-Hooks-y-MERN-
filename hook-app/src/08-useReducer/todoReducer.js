export const todoReducer = (initialState, action) => {
  switch (action.type) {
    case '':
      // return initialState;
      throw new Error('Action.type = "" no esta implementada');
    default:
      break;
  }
}